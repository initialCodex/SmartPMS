# Database Configuration (H2 for development)
spring.datasource.url=jdbc:h2:mem:smart_pms;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# Thymeleaf Configuration
spring.thymeleaf.cache=false

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.hospital.smartpms=DEBUG

# Disable Spring Boot DevTools for stable running
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false
spring.devtools.add-properties=false

# BigModel.cn AI Configuration
bigmodel.api.key=aab0d596e0354d92a14a19538795dd5b.xku7V9fc1OfSFxzp
bigmodel.api.base-url=https://open.bigmodel.cn/api/v4
bigmodel.api.model=glm-4